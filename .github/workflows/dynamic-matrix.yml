name: dynamic-matrix
on:
  workflow_dispatch:
jobs:
  generate-colors:
    runs-on: ubuntu-latest

    outputs:
      colors: ${{ steps.colors.outputs.colors }}

    steps:
      - uses: actions/checkout@v4

      - name: Define Colors
        id: colors
        run: |
          script/generate_color.sh
      - name: Debug GITHUB_OUTPUT
        run: cat $GITHUB_OUTPUT

  use-colors:
    runs-on: ubuntu-latest
    needs: generate-colors
    strategy:
      matrix:
        color: ${{ fromJSON(needs.generate-colors.outputs.colors) }}

    steps:
      - name: Report Colors
        run: |
          printf "\n\n Matrix parameters: ${{ matrix.color }}"
