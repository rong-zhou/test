name: Caller workflow
on:
  workflow_dispatch:
    inputs:
      username:
        required: true
        description: name of user
        type: string
jobs:
  login:
    uses: ./.github/workflows/reusable-workflow-1.yml

